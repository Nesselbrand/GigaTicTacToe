package IO;

import util.IntTupel;

public class In {
    public IntTupel readMove() {//x1 = x && x2 = y
        IntTupel input = new IntTupel(-1, -1); //-1 cathch error und sachen
        return input;
    }
}